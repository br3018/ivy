[project]
name = "orchestrator"
version = "0.1.0"
description = "LangGraph Orchestrator"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langgraph>=1.0.5",
    "python-dotenv>=1.2.1",
]
